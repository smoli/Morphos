/**
 * Konstanten, die der Build setzt (siehe vite.config.ts).
 *
 * `__MORPHOS_COMMIT__` ist der Commit, aus dem Morphos gebaut wurde — er steht
 * im Readme einer App (c0077). Ohne Git beim Bauen ist er leer.
 */
declare const __MORPHOS_COMMIT__: string;
